Here can you see how in React elements are inmutable, once the clock is created we can only see one static value,
that`s why we implement the setInterval callback


import React from 'react';
import ReactDOM from 'react-dom';
//function tick() {
    const element = (
      <div>
        <h1>Hello, world!</h1>
        <h2>It is {new Date().toLocaleTimeString()}.</h2>
      </div>
    );
  //}
  ReactDOM.render(element, document.getElementById('root'));
  
  // setInterval(tick, 3000); //here you can modify the updating of the rendering in ms

  